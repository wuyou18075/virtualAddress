{
  "name": "virtualaddress",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/unit.mjs",
    "check": "node --check src/worker.js && node --check src/js/main.js && node --check src/js/data-loader.js",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "4.110.0"
  }
}
